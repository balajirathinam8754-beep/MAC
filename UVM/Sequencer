class mac_sequencer extends uvm_sequencer #(mac_sequence_item);

    `uvm_component_utils(mac_sequencer)

    function new(
        string name = "mac_sequencer",
        uvm_component parent = null
    );
        super.new(name, parent);
    endfunction

endclass
